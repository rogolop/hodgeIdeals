177,0
S,MonomialSequence,"Given a sequence S of polynomials in two variables and a non-empty sequence of maximal contact elements (non-constant, irreducible), write each polynomial in S as a monomial in terms of the maximal contact elements (f1,...,fg,f) and possibly other polynomials (g1,...,gk). Also return the sequence of polynomials corresponding to g1,...,gk",0,2,0,0,0,0,0,0,0,82,,0,0,82,,82,82,-38,-38,-38,-38
S,MonomialSequence,"Given an ideal I of polynomials in two variables and a non-empty sequence of maximal contact elements (non-constant, irreducible), write each polynomial in the basis of I as a monomial in terms of the maximal contact elements (f1,...,fg,f) and possibly other polynomials (g1,...,gk). Also return the sequence of polynomials corresponding to g1,...,gk",0,2,0,0,0,0,0,0,0,82,,0,0,64,,82,82,-38,-38,-38,-38
S,MonomialSequence,"Given an ideal I of polynomials in two variables and a non-empty sequence of maximal contact elements (non-constant, irreducible), write each polynomial in the basis of I as a monomial in terms of the maximal contact elements (f1,...,fg,f) and possibly other polynomials (g1,...,gk). Also return the sequence of polynomials corresponding to g1,...,gk",0,2,0,0,0,0,0,0,0,82,,0,0,470,,82,82,-38,-38,-38,-38
